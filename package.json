{
  "name": "onepay-sdk",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "release": "turbo run build && changeset version && changeset publish",
    "prepare": "husky",
    "changeset": "changeset",
    "lint": "turbo run lint",
    "release:beta": "changeset pre enter beta && changeset version && pnpm build && pnpm publish && changeset pre exit"
  },
  "keywords": [],
  "author": "IroshaHewage",
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "@types/node": "^22.9.0",
    "husky": "^9.1.6",
    "prettier": "^3.3.3",
    "pretty-quick": "^4.0.0",
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@9.12.3",
  "pnpm": {
    "overrides": {
      "cross-spawn": "^7.0.5"
    }
  }
}
